<%= simple_form_for(@status) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, :label=>'Namn' %>
  </div>

   <div class="form-actions">
   	<%= button_to 'Åter', statuses_path, :class=>'btn btn-primary' %>
    <%= f.submit 'Spara', :class=>'btn btn-primary' %>
  </div>
<% end %>
