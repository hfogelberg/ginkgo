<h1>Gallerier</h1>

  <tbody>
    <% @collections.each do |collection| %>
      <tr>
        <td><%= link_to collection.name, edit_collection_path(collection) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Ny samling', new_collection_path %>
